server:
  port: 9000

spring:
  application:
    name: dataConnnect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://localhost:3306/springcloud?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    database: 0
    jedis:
      pool:
        max-active: 8
        min-idle: 4
        max-idle: 20
        max-wait: -1


